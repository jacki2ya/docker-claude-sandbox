# The image only needs entrypoint.sh, image/, and bin/claude-statusline
# (the in-container status line script). Everything else stays out.
.git
.gitignore
.dockerignore
README.md
LICENSE
install.sh
test.sh
bin
!bin/claude-statusline
